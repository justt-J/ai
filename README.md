create a phyton environment (python3 -m venv NAME_OFE_ENVIRONMENT)
create .env file: 
content:
    OPENAI_API_KEY = 
    NEO4J_URI =
    NEO4J_USERNAME =
    NEO4J_PASSWORD = 

install the dependencies from the notebook
